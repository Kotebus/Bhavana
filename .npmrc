# Required for EAS Build and local installs: several transitive
# peerOptional ranges across Expo SDK 56 and react-i18next 15.x do not
# fully agree on TypeScript 5 vs 6. Falling back to npm 7's legacy
# resolution matches what we use locally.
legacy-peer-deps=true
